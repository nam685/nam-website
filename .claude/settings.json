{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | grep -E '\\.py$' | { read -r f; uv run ruff check --fix \"$f\" && uv run ruff format \"$f\"; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
